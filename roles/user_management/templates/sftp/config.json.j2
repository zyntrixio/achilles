{
{% for u in sftpusers %}
{% if 'upload' in u %}
    "{{ u['name'] }}": {
        "path": "/home/{{ u['name'] }}/uploads",
        "dsn": "{{ u['upload']['conn_string'] }}",
        "container": "{{ u['upload']['container'] }}",
        "slug": "{{ u['upload']['slug'] }}"
    {{ "}," if not loop.last else "}" }}
{% endif %}
{% endfor %}
}
